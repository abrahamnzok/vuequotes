import Vue from 'vue'
import Router from 'vue-router'
import Bootstrap from 'bootstrap-vue'

Vue.use(Bootstrap);
Vue.use(Router);

export default new Router({
  routes: [
    {
      path: '/'
    }
  ]
})
